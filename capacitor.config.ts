import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.tracksoft.bizfinder',
  appName: 'bizfinder',
  webDir: 'out',
  server: {
    androidScheme: 'https'
  }
};

export default config;
